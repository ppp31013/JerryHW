def  calculator（a，b，c）：
    if（c == “ + ”）：
        return（int（a）+ int（b））
    if（c == “ - ”）：
        return（int（a）- int（b））
    if（c == “ * ”）：
        return（int（a）* int（b））
    if（c == “ / ”）：
        返回（int（a）/ int（b））
