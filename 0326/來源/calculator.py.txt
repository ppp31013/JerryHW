def  calculator（a，b，c）：
    如果（c == “ + ”）：
        return（int（a）+ int（b））
    if（c == “ - ”）：
        return（int（a）- int（b））
    如果（c == “ * ”）：
        return（int（a）* int（b））
    如果（c == “ / ”）：
        返回（int（a）/ int（b））